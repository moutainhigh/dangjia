<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dangjia.acg.mapper.actuary.DjSimulationTemplateConfigDetailMapper">


    <!-- 查询设计精算阶段的配置模板 -->
    <select id="selectCurrentIndexByTemplateId" resultType="java.lang.String">
       select IFNULL(max(template_detail_index),0)+1 from dj_simulation_template_config_detail t where t.simulation_template_id=#{templateId}
    </select>

    <select id="getSimulateionConfigDetail" resultType="com.dangjia.acg.dto.actuary.SimulationTemplateConfigDetailDTO">
        SELECT
            id,
            simulation_template_id simulationTemplateId,
            CODE,
            NAME,
            image,
            label_name labelName
        FROM
            dj_simulation_template_config_detail
        where config_status = 0
        and id=#{simulationDetailId}
    </select>

</mapper>