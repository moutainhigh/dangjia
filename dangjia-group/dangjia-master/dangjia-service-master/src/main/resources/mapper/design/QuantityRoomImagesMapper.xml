<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dangjia.acg.mapper.design.IQuantityRoomImagesMapper">


    <select id="getBillQuantityRoom" resultType="java.lang.String">
         SELECT
            b.image
        FROM
            dj_design_quantity_room a,
            dj_design_quantity_room_images b
        WHERE
            a.id = b.quantity_room_id
        AND a.house_id = #{houseId}
        AND a.type = 1
        AND a.data_status = 0
        ORDER BY
            a.create_date
        LIMIT 1
    </select>
</mapper>

