spring:
  application:
    name: dangjia-eureka
  cloud:
    config:
      uri: http://config.fengjiangit.com
  profiles:
    active: dev
endpoints:
  shutdown:
    enabled: true
    sensitive: false
